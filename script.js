document.addEventListener('DOMContentLoaded', () => {
    const watchNowButton = document.querySelector('.hero button');
    watchNowButton.addEventListener('click', () => {
        alert('Watch Now button clicked!');
    });
});
